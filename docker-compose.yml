version: "1.0.0"
services:
  bot:
    network_mode: bridge
    depends_on:
      - db
    links:
      - db
    build: .

  db:
    image: postgres
    restart: always
    network_mode: bridge
    environment:
      POSTGRES_USER: io
      POSTGRES_DB: io
      POSTGRES_PASSWORD: io
